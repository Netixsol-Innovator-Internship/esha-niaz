# Base URL of your backend API (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Socket.io URL (can be same as API)
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# If your backend sets httpOnly cookies for auth, set this to true and leave token handling to cookies
NEXT_PUBLIC_AUTH_USE_COOKIES=false
